{
  "name": "rogue-hero-4",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "electron-main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "electron": "electron .",
    "start": "npm run build && electron .",
    "standalone": "npm run build && electron .",
    "smoke": "npm run build && electron scripts/smoke-electron.cjs",
    "beauty": "npm run build && electron scripts/smoke-electron.cjs",
    "verify": "tsc --noEmit && vite build",
    "package": "npm run build && electron-builder --win portable",
    "dist": "npm run build && electron-builder --win nsis"
  },
  "author": "Justin Kramer",
  "build": {
    "appId": "com.roguehero.causeway",
    "productName": "Rogue Hero 4",
    "files": ["electron-main.cjs", "electron-ipc.cjs", "preload.cjs", "dist/**/*", "package.json"],
    "directories": { "output": "release" },
    "win": { "target": ["nsis"] },
    "portable": { "artifactName": "Rogue-Hero-4-${version}-portable.exe" },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "Rogue Hero 4"
    }
  },
  "dependencies": {
    "@fontsource/cinzel": "^5.2.8",
    "@fontsource/rajdhani": "^5.2.7",
    "postprocessing": "^6.36.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "electron": "^42.4.0",
    "electron-builder": "^25.1.8",
    "playwright-core": "^1.60.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
